//Operadores Aritméticos
const [a, b, c, d] = [3, 5, 1, 30]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 //resto da divisão; = 0 (par); = 1 (ímpar)

console.log(soma, subtracao, multiplicacao, divisao, modulo)
